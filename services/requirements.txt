# What the service image installs. Pinned, and installed at build time: the
# runtime never reaches a package index.
-r common/requirements.txt

fastapi==0.141.1
starlette==1.6.0
uvicorn[standard]==0.53.0

# Prometheus instrumentation (services/common/metrics.py). It lives here and
# not in common/requirements.txt because it is a service-layer concern: the
# shared library defines the metrics, but only a running service exports them.
prometheus-client==0.26.0
