[pytest]
# `services` is a plain package directory on the path, not an installed
# distribution: the containers copy it in, and the tests import it the same way.
pythonpath = .
testpaths = tests/unit
addopts = -q
